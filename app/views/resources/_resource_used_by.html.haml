.content-t-and-c
  - if resource.resourceables != nil && resource.resourceables != []
    Resource belongs to
    %div
      - started = false
      - res_list = resource.resourceables.select!{ |r| r} || []
      - res_list.sort{ |a,b| a.name <=> b.name }.each do |r|
        = ', ' if started
        - started = true
        - if r.is_a?(Page)
          = link_to r.name, page_path(r)
        - else
          = link_to r.name, collection_item_path(r)
  - else
    This is not a resource for any collection items or information pages yet, edit to associate with these
.color-bbb
  - #todo do we want an identification here?
  = #"Resource created by #{resource.user.name}"